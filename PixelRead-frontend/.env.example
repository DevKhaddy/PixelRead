# Public URL of the deployed capture API (no trailing slash).
# The dashboard calls <VITE_API_URL>/api/screenshot for real captures.
# Example: VITE_API_URL=https://pixelread-api.onrender.com
VITE_API_URL=
